# -*- coding: utf-8 -*-
import numpy as np
import matplotlib.pyplot as plt
import astropy.io 

import sncosmo
from sndata.jla import Betoule14


jla_data = Betoule14()
#data.download_module_data()

#for data in jla_data.iter_data():

# Get a list of available objects
list_of_ids = jla_data.get_available_ids()

# Get data for a given object
demo_id = list_of_ids[0]
data_table = jla_data.get_data_for_id(demo_id)
print(data_table)

# Don't forget to check the meta data!
print(data_table.meta)